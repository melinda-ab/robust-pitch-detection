# Core scientific stack
numpy>=1.24
scipy>=1.10
librosa>=0.10
soundfile>=0.12
matplotlib>=3.7
tqdm>=4.65

# Optional pitch detectors (install at least one)
# swiftf0          # pip install swiftf0          (primary target)
# crepe            # pip install crepe             (secondary fallback)
# If neither is installed, librosa pYIN is used automatically.

# For GPU-accelerated CREPE (optional)
# tensorflow>=2.13  or  torch>=2.0